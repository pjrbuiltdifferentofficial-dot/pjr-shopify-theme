<!doctype html>
<html lang="{{ request.locale.iso_code }}">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <title>{{ shop.name }}</title>
    {{ 'theme.css' | asset_url | stylesheet_tag }}
    {{ content_for_header }}
  </head>
  <body>
    <div class="empty">
      <p class="wordmark__pjr" style="font-family:var(--font-display);letter-spacing:0.18em;">PJR</p>
      <p class="kicker" style="margin-top:1rem;">Coming. No days off.</p>
      {{ content_for_layout }}
    </div>
  </body>
</html>
