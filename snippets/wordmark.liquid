<a href="{{ routes.root_url }}" class="wordmark{% if compact %} wordmark--compact{% endif %}">
  <span class="wordmark__pjr">PJR</span>
  {% unless compact %}
    <span class="wordmark__sub">Built Different</span>
  {% endunless %}
</a>
